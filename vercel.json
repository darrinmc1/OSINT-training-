{
  "buildCommand": "cd osint-academy && npm install && npm run build",
  "outputDirectory": "osint-academy/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}