{
  "buildCommand": "cd osint-academy && npm install --legacy-peer-deps && npm run build",
  "outputDirectory": "osint-academy/dist",
  "framework": "vite",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}